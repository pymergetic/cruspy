openapi: "3.1.0"
info:
  title: Metadata Model
  version: "1.0.0"

x-cruspy-namespace: pymergetic.cruspy.models.document.metadata

components:
  schemas:
    Metadata:
      type: object
      description: "Reusable metadata block."
      properties:
        id:
          type: integer
          format: int32
          default: 0
          minimum: 0
          maximum: 100
          description: "Metadata record id."
        created_at:
          type: integer
          format: int64
          default: 0
          description: "Unix timestamp (seconds)."
